# 易因 · 世界分析引擎 — 更新日志

## v4.5.4 (2026-05-15) — 知识视图布局修复：嵌套 Grid 清理与全宽突破

### Bug 修复
- **修复四大知识视图内容错乱**：移除 HTML 中 `#phenomenologyGrid`、`#praxisGrid`、`#contradictionGrid`、`#stoicGrid` 外层的 `knowledge-grid` 类，消除 JS 渲染时产生的嵌套 Grid 问题
- **卡片最小宽度保护**：`.knowledge-card` 添加 `min-width: 280px; max-width: 100%`，防止内容挤压变形

### 布局优化
- **知识视图全宽突破**：`.content` 恢复 `max-width: 760px`（首页观感不变），知识视图通过负 margin 计算占满 `100vw - sidebar` 主区域
- **自适应列数**：`.knowledge-grid` 使用 `repeat(auto-fill, minmax(280px, 1fr))`，大屏 4-5 列，小屏自动降级
- **移动端单列**：560px 以下自动切换单列布局

## v4.5.3 (2026-05-15) — 知识页面卡片统一配色与布局

### 配色统一
- **删除各视图独立配色**：移除现象学（青色）、实践论（红色）、矛盾论（红色）、斯多葛（绿色）等独立颜色主题
- **统一为系统 accent 金色系**：所有知识视图的 `knowledge-icon`、`knowledge-header`、`knowledge-card` 统一使用 `#c9a96e` 金色渐变
- **统一 card num 样式**：`knowledge-card-num` 使用 `var(--surface-subtle)` 背景 + `var(--accent-400)` 文字

### 布局优化
- **卡片宽度自适应**：`.knowledge-grid` 使用 `repeat(auto-fill, minmax(280px, 1fr))`，从左到右、从上到下铺满屏幕
- **统一 hover 效果**：所有卡片 hover 时统一使用 `border-color: var(--border-accent)` + `box-shadow: var(--shadow-md)`

## v4.5.2 (2026-05-15) — Markdown 解析修复

### 格式解析优化
- **修复 `####` 四级标题未解析**：`markdownToHtml()` 新增 `####` 匹配规则，并调整替换顺序确保长匹配优先
- **统一标题字号层级**：h1 20px、h2 17px、h3 15px、h4 14px，层次分明

## v4.5.1 (2026-05-15) — 分析结果增加摘要层级

### 输出格式优化
- **两段式分析结构**：修改 `buildUserPrompt()`，要求 LLM 先输出「整体摘要（必读）」，再输出「详细分析（按需阅读）"
- **降低阅读成本**：摘要部分控制在 3-5 句话，概括核心本质、关键卡点和最重要行动建议
- **详细内容按需展开**：用户在摘要下方可继续阅读现象学、易经、十二因缘、马克思主义、斯多葛等维度的逐层解读

## v4.5.0 (2026-05-15) — 配置界面隐藏 + 全页流式输出

### 分析流程彻底重构
- **配置界面自动隐藏**：点击「开始分析」后，hero 标题 + 输入框 + 维度选择器全部隐藏，页面直接进入结果阅读模式
- **全页流式输出**：LLM 分析结论不再在弹窗中显示，而是在主页面 `#results` 区域全宽流式输出，支持 Markdown 渲染
- **新增「新建分析」按钮**：结果区域顶部提供返回配置界面的入口，方便开始新的分析
- **删除弹窗分析模式**：移除 `showAnalysisModal()` 调用，分析流程完全内嵌在页面主体中

### 历史记录查看优化
- **直接显示报告**：点击历史记录后配置界面自动隐藏，直接展示保存的 LLM 分析结果
- **无闪烁切换**：`loadHistory()` 直接操作 DOM 显示 analyze 视图，避免 switchView 带来的界面闪烁

## v4.4.0 (2026-05-15) — 移除知识卡片 + 历史记录直接查看结果

### 页面下方知识卡片删除
- **大幅简化 `renderResult()`**：删除所有本地结构化卡片渲染（现象学、易经、十二因缘、实践论、矛盾论、斯多葛、交叉分析、行动建议）
- **关闭弹窗后不再渲染卡片**：`closeAnalysisModal()` 和 `runLocalAnalysis()` 移除自动渲染逻辑
- **analyze 页面更简洁**：点击分析后仅通过弹窗展示 LLM 深度解读，页面下方保持干净

### 历史记录直接查看结果
- **重写 `loadHistory()`**：点击历史记录不再切换到 analyze 视图，而是直接打开弹窗显示保存的分析结果
- **新增 `showSavedAnalysisModal()`**：展示历史分析结果弹窗，包含分析时间、卦象、现象描述和 LLM 分析内容
- **无结果记录友好提示**：若历史记录暂无 LLM 分析内容，弹窗显示占位提示

## v4.3.9 (2026-05-15) — 分析流程重构：弹窗聚焦 + LLM 流式分析

### 弹窗式深度分析
- **新增分析弹窗**：点击「开始分析」后焦点转移至全屏弹窗，原页面以模糊遮罩退居背景
- **输入摘要面板**：弹窗顶部展示用户输入摘要（现象、分析维度、现象类型），可折叠/展开
- **LLM 流式输出**：弹窗主区域实时流式显示 AI 深度分析结论，支持 Markdown 渲染
- **操作按钮栏**：底部提供「重新分析」「复制结果」「关闭」按钮

### 分析逻辑重组
- **新增 `buildUserPrompt()`**：直接从用户输入组装 LLM 提示词，不依赖本地分析结果
- **重写 `analyze()`**：改为打开弹窗 + 后台异步执行本地 11 步分析
- **新增 `runLocalAnalysis()`**：本地分析在后台静默执行，弹窗关闭后渲染结构化卡片
- **ESC 键支持**：按 ESC 可关闭分析弹窗

### 防御性修复
- 弹窗初始化外层包裹 try/catch，DOM 元素增加 null 校验
- `streamLLM` 增加对未知 LLM provider 的检测与友好提示

## v4.3.8 (2026-05-15) — Bug 修复与交互优化

### 分析流程进度跟踪
- **新增 11 步进度面板**：点击「开始分析」后实时展示每一步执行状态
  1. 匹配现象学维度 → 2. 推演易经卦象 → 3. 分析十二因缘 → 4. 计算动爻变卦 → 5. 匹配实践论阶段 → 6. 分析矛盾结构 → 7. 匹配斯多葛原则 → 8. 生成交叉分析 → 9. 生成行动建议 → 10. 保存结果 → 11. 渲染结果
- **步骤完成标记**：已完成的步骤显示绿色 ✓，当前步骤高亮，未到步骤置灰
- **错误定位**：若某步失败，进度面板底部显示红色错误框，精确告知失败原因
- **延时优化**：分析延时从 800ms 降至 100ms，减少等待感

### 缺失函数补全
- **新增 `determineMovingYao(scenario)`**：基于输入文本哈希生成 1-6 动爻，确保每次相同输入得到一致动爻
- **新增 `calculateChangedGua(gua, movingYao)`**：根据动爻位置翻转阴阳爻，在 64 卦中查找匹配的变卦，符合易经原理
- **新增 `analysisDepthConfig` + `getAnalysisDepth()`**：三级分析深度配置（简洁/标准/深度），修复之前 `analysisDepthConfig` 未定义导致的分析中断

### 知识页面修复
- **修复 5 个知识页面空白**：现象学、实践论、矛盾论、斯多葛、易经、十二因缘页面新增独立 View 容器
- **新增 `switchView()` 路由**：侧边栏导航正确切换至各知识页面
- **修复数据字段映射**：
  - `praxisData` 使用 `stage` 字段排序
  - `contradictionData` 使用 `stage` 字段排序
  - `phenomenologyData` 使用 `order` 字段排序
  - `stoicData` 使用 `order` 字段排序
- **新增 `matchGua()` / `matchPratitya()`**：卦象和十二因缘的关键词匹配算法，与现有 `matchPraxis()` 保持一致

### 数据字段安全修复
- **修复 `stoic.primary.questions` 未定义错误**：stoicData 无 `questions` 字段，替换为 `practice`
- **防御式访问**：`phenomenology.primary.questions` 和 `praxis.primary.questions` 添加存在性检查后再调用 `.map()`
- **修复 `crossAnalysis4D` 模板**：第三套模板中斯多葛引用从 `questions[0]` 改为 `practice`

### Service Worker 修复
- **跳过跨域请求拦截**：fetch 事件开头判断 `event.request.url.includes('api.')` 直接 `return`，避免 LLM API 请求被 SW 拦截导致 `Returned response is null`
- **保持缓存策略不变**：静态资源仍使用 stale-while-revalidate

### LLM 连接修复
- **新增 OpenRouter 提供商**：Kimi/DeepSeek 官方 API 无浏览器 CORS 支持，添加 OpenRouter 作为直连备选
- **请求头配置**：OpenRouter 需 `HTTP-Referer` 和 `X-Title` headers
- **流式输出补充**：`streamLLM()` 函数补全，支持 SSE 流式解析

### 功能删除
- **删除实时匹配能力**：移除 `updateLivePreview()` 函数及所有调用点，输入时不再实时显示匹配结果
- **删除 livePreview DOM 容器**：index.html 中移除 `#livePreview` 元素

### 代码健壮性
- **analyze() 加 try/catch/finally**：确保即使中间步骤抛错，分析按钮状态也能恢复，错误信息通过 `showProgressError()` 展示在 UI 上

---

## v4.1.0 (2026-05-12) — Round 6：LLM 深度解读 + 变卦逻辑修正（内核升级）

### LLM 深度解读（核心升级）
- **接入 Kimi K2.6 / DeepSeek V4 Pro**：在设置面板配置 API Key 后，分析结果会额外生成 AI 深度解读
- **流式输出**：实时打字机效果，边生成边显示，无需等待
- **智能 Prompt 工程**：将卦象数据（卦名、卦义、位置、危险、转化方向、动爻、变卦）+ 十二因缘数据（主节点、表现、突破点）+ 用户场景 组合成结构化 prompt
- **三层分析框架**：
  1. 规律层面（易经）：系统状态与能量流动
  2. 人性层面（十二因缘）：核心驱动机制与卡点
  3. 行动层面：3-5 条具体可操作的干预建议
- **本地优先**：API Key 仅存储在 IndexedDB，所有调用从浏览器直接发出
- **一键复制**：深度解读支持复制到剪贴板
- **失败重试**：连接失败时显示错误信息和重试按钮

### 变卦逻辑修正
- **修复错误的变卦算法**：原实现 `(currentIndex + movingYao) % 64` 只是数组循环，完全不符合易经原理
- **真正的阴阳爻翻转**：根据动爻位置，翻转对应上下卦中的阴阳爻位
- **八卦二进制映射**：乾(111) 兑(110) 离(101) 震(100) 巽(011) 坎(010) 艮(001) 坤(000)
- **动态查卦**：翻转后根据新的上下卦组合在 64 卦中查找匹配的卦象

### 设置面板升级
- 新增「AI 深度解读」设置区块
- 支持切换 Kimi / DeepSeek 提供商
- API Key 输入框（password 类型，本地存储）
- 连接测试按钮

### 样式
- LLM 结果卡片：琥珀金边框渐变背景，与常规结果区分
- 流式加载动画：三脉冲点
- Markdown 渲染：标题、加粗、列表自动转 HTML

---

## v4.0.0 (2026-05-12) — Round 6：智能分析引擎与交互升级

### 分析深度系统
- **三级分析深度**：简洁 / 标准 / 深度，通过 `analysisDepthConfig` 配置不同深度的输出策略
- **简洁模式**：仅输出核心摘要，隐藏六爻和次要卡点，适合快速浏览
- **标准模式**：完整展示六爻、变卦、主要/次要卡点，适合日常使用
- **深度模式**：额外输出「情境觉察」板块，包含决策层面分析和危险警示，适合重要决策
- **快捷键 ⌘D**：快速循环切换分析深度，实时生效
- **命令面板入口**：Command+K 搜索「切换分析深度」或「导入数据」

### 交叉分析数据库大幅扩充
- **从 7 条扩充到 100+ 条**：覆盖所有 64 卦 × 主要因缘卡点的组合
- **卦象-因缘精准映射**：每卦与无明、行、识、受、爱、取等核心卡点都有专属解读
- **动态模板回退**：未覆盖的组合使用 4 套动态模板，基于哈希值确定性选择，保证相同输入输出一致
- **深度感知**：深度模式下交叉分析文本更详尽，简洁模式更精炼

### 卦象匹配算法增强
- **类型加权系统**：personal/relationship/business/social/creative/political 六大类型各有推荐卦象权重加成
- **更精准的场景匹配**：例如 relationship 类型会加权泰、咸、恒、睽、家人等卦

### 一键复制结果
- **每个结果卡片头部新增复制按钮**：卦象、十二因缘、交叉分析、干预建议均可独立复制
- **格式化文本输出**：复制内容包含板块标题和结构化信息，方便粘贴到笔记或聊天
- **Toast 反馈**：复制成功后显示「已复制到剪贴板」

### 数据导入功能
- **JSON 备份导入**：支持导入之前导出的备份文件，自动合并到现有历史记录
- **文件选择器**：点击命令面板「导入数据」或设置面板按钮触发
- **智能校验**：检查文件格式有效性，过滤无效记录
- **导入统计**：显示成功导入的记录数量

### 干预建议深度适配
- **简洁模式**：2 条核心建议（状态调节 + 断点干预）
- **标准模式**：4 条建议，增加变卦启示和交叉策略
- **深度模式**：6 条建议，额外包含辅助干预和情境觉察

---

## v3.2.0 (2026-05-12) — Round 5：图片导出重构与交互精细化

### 图片导出完全重构
- **Canvas 2D 原生渲染**：替换不可靠的 SVG foreignObject 方案，使用纯 Canvas 2D API 绘制分析结果图片
- **高分辨率输出**：2x 缩放确保导出图片清晰锐利，适合社交媒体分享
- **专业报告排版**：包含标题、时间戳、场景摘要、卦象详情、因缘分析、交叉分析、干预建议完整结构
- **中文文本自动换行**：`wrapText` 函数实现中文字符级自动换行，避免截断
- **动态高度裁剪**：根据实际内容高度裁剪画布，避免大量空白

### 结果卡片交互升级
- **折叠状态持久化**：从 sessionStorage 升级为 localStorage，刷新页面后折叠偏好依然保留
- **智能卡片类型识别**：新增 `getCardType()` 函数，根据卦符内容自动识别卡片类型（卦象/因缘/交叉/建议）
- **展开动画增强**：卡片展开时触发 `fadeIn` 动画，内容平滑显现
- **左侧彩色指示条**：鼠标悬停时显示渐变竖线指示条，交叉分析卡片使用绿-蓝渐变
- **箭头指示器优化**：使用绝对定位确保箭头始终在最右侧，旋转动画更流畅

### 快捷键帮助面板样式
- **三列网格布局**：导航、操作、导出三大分类清晰排列
- **分组标题样式**：金色大写字母 + 下划线分隔
- **KBD 按键样式**：仿 macOS 按键设计，带阴影和边框
- **响应式适配**：移动端自动切换为单列布局

### 数据层修复
- **时间戳类型兼容**：`formatRelativeTime` 现在同时兼容字符串和数字类型的时间戳，修复 IndexedDB 返回 ISO 字符串导致的时间计算错误
- **历史记录时间分组**：今天/昨天/本周/更早的分组逻辑恢复正常

### CSS 清理
- 移除重复的旧版折叠卡片样式，避免选择器冲突
- 统一折叠动画使用 `cubic-bezier(0.16, 1, 0.3, 1)` 缓动曲线

---

## v3.1.0 (2026-05-12) — Round 4：商业化功能完备化

### PWA 与离线支持增强
- **Service Worker 升级至 v4**：实现 stale-while-revalidate 缓存策略，静态资源优先从缓存读取同时后台更新，确保离线可用且内容新鲜
- **新增 Background Sync 支持**：为离线分析队列预留同步接口
- **导航请求网络优先**：确保在线时总是获取最新版本，离线时回退到缓存

### 新增 PDF 导出功能
- **纯前端 PDF 生成**：使用浏览器打印到 PDF 机制，打开精美的打印预览窗口
- **A4 优化排版**：专业的报告样式，包含页眉、章节、页脚
- **完整报告内容**：卦象释义、十二因缘分析、交叉分析、干预建议全部包含
- **快捷键 ⌘⇧P** 快速导出

### 新增分享链接功能
- **URL Hash 编码分享**：将分析结果编码到 URL hash 中，生成可分享的链接
- **接收分享链接**：访问带 `#share=` 的链接自动加载对应分析
- **剪贴板优先**：优先复制链接，失败时回退到文本摘要

### 新增设置面板
- **设置模态框**：⌘, 快捷键或点击侧边栏底部打开
- **自动保存草稿**：可开关，默认开启
- **动画效果**：可开关，影响所有过渡动画
- **紧凑模式**：减小间距和字体，适合小屏幕
- **数据导出/清除**：支持完整数据备份 JSON 导出，一键清除所有数据

### 草稿自动保存系统
- **IndexedDB 草稿存储**：输入后 1 秒自动保存到 IndexedDB
- **7 天草稿恢复**：页面加载时自动恢复 7 天内的草稿
- **完整状态恢复**：恢复文本、标签、分析维度、现象类型
- **清空时删除草稿**：点击清空按钮同时清除草稿存储

### 分析结果卡片可折叠
- **点击标题折叠/展开**：每个结果卡片均可独立折叠
- **平滑动画过渡**：max-height + opacity 组合实现流畅展开/收起
- **箭头指示器**：折叠状态通过旋转箭头直观展示

### 视觉升级
- **动态背景粒子**：12 个浮动光粒子，营造空间感和沉浸感
- **尊重减少动画偏好**：`prefers-reduced-motion` 自动禁用粒子
- **增强玻璃态效果**：result-card、input-card、modal-panel、cmd-panel 全部升级为多层玻璃态

---

## v3.0.0 (2026-05-12) — Round 3：深度重构与数据层修复

### 核心修复
- **修复 guaData 重复 key 问题**：为所有重复卦分配唯一 key（`bi_gua`、`bi2`、`jian2`、`jian3`、`qian2` 等），确保 64 卦数据完整
- **修复 guaPatterns 重复 key**：同步去重，确保关键词匹配算法正确命中所有 64 卦
- **修复 pratityaData 缺少 color 字段**：为 12 个节点补充 `color` 字段

### 架构升级
- **CSS 独立拆分**：~2350 行 CSS 提取为独立 `styles.css`
- **JS 独立拆分**：~1600 行 JS 提取为独立 `app.js`
- **index.html 精简**：从 4122 行 / 206KB 精简为 ~437 行 / 26KB

### 存储层升级
- **IndexedDB 替换 localStorage**：历史记录、收藏全面迁移至 IndexedDB（`yiyin_db`）
- 新增 `dbGetAll`、`dbPut`、`dbDelete`、`dbClear`、`dbGet` 等通用工具函数

### 新增功能
- **图片导出**：纯原生 Canvas 2D API 导出 PNG
- **时间起卦动爻与变卦**：根据输入哈希计算动爻，推演变卦
- **六爻可视化**：阴阳爻条、爻辞、动爻高亮
- **标签系统**：Enter/逗号添加、Backspace 删除

---

## v2.0.0 (2026-05-12) — Round 2：基础框架
- 基础 UI 框架搭建
- 关键词匹配算法
- 基础历史记录

## v1.0.0 (2026-05-12) — Round 1：项目初始化
- 单文件 HTML 原型
- 基础卦象数据
